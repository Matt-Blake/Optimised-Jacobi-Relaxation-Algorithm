{
	"version": "2.0.0",
	"miDebuggerPath": "/usr/local/bin/GDB",
	"tasks": [
		{
			"type": "shell",
			"label": "build",
			"label": "C/C++: g++ build active file",
			"args": [
				"-g",
				"${file}",
				"-o",
				"${fileDirname}/${fileBasenameNoExtension}"
			],
			"options": {
				"cwd": "${workspaceFolder}"
			},
			"problemMatcher": [
				"$gcc"
			],
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"osx": {
				"command": "/usr/bin/g++",
			},
			"linux": {
				"command": "",
			}
		}
	]
}